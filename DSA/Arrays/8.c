// Arrays in compilers
#include<stdio.h>
int main(){
    int A[5]= {3,5,8,4,2};
    A[3] = 0; 
    // compiler converts the code to machine code,
    // for that compiler uses a formula to calculate address of any location of array element or array
    // address(A[i])= Base address(L0 + i *w(w = size of data type)
    
}