// if in any lang , index starts from 1
#include<stdio.h>
int main(){
    int A[5];
    // int A[1..5]
    // this time formula will become,
    // Addr(A[i]) = L0 + (i-1)*w
    // it will have more operatiosn and comparatively slower than c and c++
}